For this sql command, the first three commands are test commands that allow you to run two simple SELECT queries that have where clauses attached to it and the third one consists of join queires as well. You must run these commands to see how the database traverses itself and the data within. Then you run the three create index statements to change how the database traverses the data. This will also allow us to make the index searching more efficient. After I did the indexing creations, the indexes changed from index scan to seq scan for a lot of the queries. The cost also went down as well which showed an increase in efficiency  
